import React, {Component} from 'react';

import Title from '../Title';

export default class Routes extends Component {
  render() {
    return (
      <div>
        <Title>Routes</Title>
        <div>Routes doing route things.</div>
      </div>
    );
  }
}
