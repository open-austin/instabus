import React from 'react';

import { NOT_FOUND_TAB } from 'constants/TabNames';

const NotFound = () => (
  <div>404 Not found</div>
);

export default NotFound;

NotFound.TAB_NAME = NOT_FOUND_TAB.name;
